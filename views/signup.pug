extends ./layouts/main.pug


block body
  form.form-signin(action='/signup', method='POST', style='width: 100%;')
    .text-center.mb-4
      h1.h3.mb-3.font-weight-normal Sign Up
    .form-label-group
      label(for='inputName') Name
      input.form-control(type='text', name='name', placeholder='Name', required='', autofocus='')
    .form-label-group
      label(for='inputEmail') Email
      input.form-control(type='text', name='email', placeholder='Email', required='', autofocus='')
    .form-label-group
      label(for='inputPassword') Password
      input.form-control(type='password', name='password', placeholder='Password', required='')
    .form-label-group
      label(for='inputPasswordCheck') Password Check
      input.form-control(type='password', name='passwordCheck', placeholder='Password Check', required='')
    br
    button.btn.btn-lg.btn-primary.btn-block(type='submit') Submit
    .text-center.mb-4
      p
        a(href='/signin') Sign In
    p.mt-5.mb-3.text-muted.text-center © 2017-2018